package com.roded

/**
 *
 * @author roded
 */
class Test9 extends BaseGebSpec {

    void "t0"() {
        when: "when..."
        Thread.sleep(SLEEP_MS)
        then: "then..."
    }

    void "t1"() {
        when: "when..."
        Thread.sleep(SLEEP_MS)
        then: "then..."
    }

    void "t2"() {
        when: "when..."
        Thread.sleep(SLEEP_MS)
        then: "then..."
    }

    void "t3"() {
        when: "when..."
        Thread.sleep(SLEEP_MS)
        then: "then..."
    }

    void "t4"() {
        when: "when..."
        Thread.sleep(SLEEP_MS)
        then: "then..."
    }

    void "t5"() {
        when: "when..."
        Thread.sleep(SLEEP_MS)
        then: "then..."
    }

    void "t6"() {
        when: "when..."
        Thread.sleep(SLEEP_MS)
        then: "then..."
    }

    void "t7"() {
        when: "when..."
        Thread.sleep(SLEEP_MS)
        then: "then..."
    }

    void "t8"() {
        when: "when..."
        Thread.sleep(SLEEP_MS)
        then: "then..."
    }

    void "t9"() {
        when: "when..."
        Thread.sleep(SLEEP_MS)
        then: "then..."
    }
}
